name: release-please

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      my-project--tag_name: {{ steps.release.outputs.my-project__tag_name }}
      my-project--release_created: {{ steps.release.outputs.my-project__release_created }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          # this assumes that you have created a personal access token
          # (PAT) and configured it as a GitHub action secret named
          # `MY_RELEASE_PLEASE_TOKEN` (this secret name is not important).
          token: ${{ secrets.GITHUB_TOKEN }}
          # this is a built-in strategy in release-please, see "Action Inputs"
          # for more options
          release-type: simple

  # release-my-project:
  #   if: ${{ needs.release-please.outputs.my-project--release_created }}
  #   runs-on: ubuntu-latest
  #   needs: release-please
  #   steps:
  #     uses:
